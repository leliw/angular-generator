package ${package}.${item};

import javax.persistence.Entity;
import javax.persistence.Id;
import javax.persistence.Table;

@Entity
@Table(name="${item}")
public class ${Item} {
#foreach($field in $javaFields.keySet())
#if($field == $id)
	@Id
#end
	public $javaFields.get($field) ${field};
#end
}
